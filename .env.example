# Cryptonite – Environment Variables
# Copy this file to .env and fill in the values. Never commit .env.

# CoinGecko API base URL (public, no key required).
VITE_COINGECKO_API_URL=https://api.coingecko.com/api/v3

# OpenAI API key (required for AI Recommendation). Get one at https://platform.openai.com/api-keys
VITE_OPENAI_API_KEY=YOUR_API_KEY_HERE

# OpenAI model for AI Recommendation (e.g. gpt-4, gpt-4o-mini).
VITE_AI_MODEL=gpt-4

# CryptoCompare: Real-Time Reports use min-api.cryptocompare.com (no key in code).
# Optional: add VITE_CRYPTOCOMPARE_API_KEY later if you use a key for higher rate limits.
